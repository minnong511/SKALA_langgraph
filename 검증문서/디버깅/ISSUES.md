# 오류 및 수정 추적

검증일: 2026-09-22 KST

## 최근 확인: 사용자 데모 실행 성공

- 실행: `python main.py --demo`, run ID `20260921T213649-e5d155d9`
- 사용자 로그와 저장된 `result.json`을 함께 확인했습니다. 새 API 호출이나 데모 재실행은 하지 않았습니다.
- 기술 조사, 시장, 이해관계자, 도메인, 근거 검증, 종합, 보고서 단계가 모두 `completed`입니다.
- Supervisor 최종 검토: `passed=true`, `issues=[]`. 모든 작업의 추가 재시도는 0회입니다.
- 최종 `needs_review`는 예시 데이터에 대한 의도된 표시이며 오류가 아닙니다.
- PDF, Markdown, 결과 JSON, 이벤트 로그 파일의 존재를 확인했습니다.
- PDF 메타데이터: 2페이지, AppleGothic, SUMMARY 반 페이지 제한 통과.
- [데모 검증 요약](../테스트/실행결과/2026-09-22-user-demo-review.json), [단계별 검증표](../테스트/단계별검증.md)
- 실제 논문 RAG와 실제 에이전트 전체 API 실행은 아직 검증하지 않았습니다.

## 오류 이력

### 2026-09-22 호출 예산 및 불필요한 요청 개선

두 번째 중단 실행 `20260921T220955-cc10f84a`: 370.935초, agent 6 + llm 16 + search 46 + source_read 132 = 200회.
기술/시장/이해관계자/도메인에서 근거 16/15/17/10개 확보 후에도 재검색을 반복해 후속 단계 미진입.

| 우선순위 | 변경 | 구현 및 검증 |
|---|---|---|
| 선행 | 총 호출 200 → 400 | `.env`의 MAX_TOTAL_CALLS만 수정, Settings 및 ExecutionLimits 기본값 400 |
| 1 | 원문 중복 읽기 축소 | 실행별 공유 캐시, 같은 URL/PDF 페이지의 병렬 요청 단일화, 검증 단계는 캐시 우회 |
| 2 | 출처 연결 오류의 재검색 분리 | 정확한 발췌 ID로 출처/URL 연결 보정, 추가 보정 필요 시 기존 자료로 LLM 최대 1회. 불명확한 근거는 계속 거부 |
| 3 | 효과 없는 재검색 중단 | 추가 검색에 새 원문이 없으면 중단. 근거와 판단 확보 시 추가 조사 최대 1회, 미확인 정보는 한계로 유지 |
| 4 | 초기 조사량 축소 | 회차별 질문 최대 3개, 질문/도구당 성공 원문 읽기 최대 3개, 조사 실행당 원문 최대 12개. 초기 원문 실패 시 검색 결과 내 다음 후보 확인 |
| 5 | 실패 원문 반복 요청 차단 | 실행 중 조사 캐시에 실패도 보관. 검증 단계에서는 실패한 원문도 독립 재확인 |
| 보호 | 후속 단계 예산 보존 | 기본 300회 사용 이후 조사 호출 중단, 남은 100회는 Supervisor/검증/종합/보고서 사용 가능 |

`partial`이라는 이유만으로 Supervisor가 전체 관점을 재조사하지 않도록 변경.
유효 근거나 판단 부재, 명시적 검증 지적에 대한 재조사 유지. 조사 예산 소진 시 재조사 배정 중단.
Verification은 실제 카드가 인용한 출처만 다시 읽되 원문 일치와 의미 검증 유지.
호출 한도가 작은 설정에서는 보존량을 총 한도의 25% 이내로 제한.

설정 키: `RESERVED_FINAL_CALLS`, `RESEARCH_QUERY_LIMIT`, `RESEARCH_READ_LIMIT`, `RESEARCH_SOURCE_LIMIT`.
기본값: 각각 100, 3, 3, 12. 별도 `.env` 값이 없으면 기본값 적용.
캐시는 프로세스의 실행 문맥에 한정, 파일 저장 없음. 호출량 집계는 실제 읽기 시에만 증가.

검증: [82 passed, 1 skipped](../테스트/실행결과/2026-09-22-research-budget.txt).
대체 도구 통합 검사에서 원문 읽기 5회 → 2회, 독립 검증 유지.
DBG-005 상태: 기능 수정 및 자동 검증 완료, 실제 전체 실행으로 완주 여부 추가 확인 필요.

### 2026-09-22 실제 조사 중단 및 인용 진단

대상 실행: `20260921T215747-08f239fd`, 303.077초, 최종 `needs_review`

| ID | 상태 | 원인과 근거 | 조치 |
|---|---|---|---|
| DBG-005 | 미해결 | agent 8 + llm 16 + search 50 + source_read 126 = 총 호출 한도 200회 소진, 시간 한도 1800초 미도달 | PDF 페이지 중복 읽기 축소 적용, 후속 단계 예산 배분과 전체 실행 검증 필요. 한도 값 변경 없음 |
| DBG-006 | 해결 | 동일 저장 자료를 이용한 별도 재현 호출에서 제안 인용 6개 모두 `...`를 포함한 비연속 문장 결합, 연속 원문 대조 0/6 통과 | 원문 발췌 ID 선택 후 코드가 해당 원문으로 evidence_text 확정, 원문 일치 검증 유지 |
| DBG-007 | 해결 | 청크 ID마다 전체 PDF 페이지 재독출. 기술 조사 2회차 39개 레코드가 실제로 21페이지, 텍스트 199116자 중 고유 페이지 91351자 | 동일 조사 실행 내 파일, 지문, 페이지가 같은 원문은 한 번 읽고 한 출처로 전달 |
| DBG-008 | 해결 | 거부된 인용문 미보존으로 이전 실패 원인의 개별 역추적 불가 | 작업별 결과의 `data.citation_audit`에 제안 인용, 출처 ID, 발췌 ID, 분류, 거부 사유 기록. 기존 비밀값 마스킹 적용 |

PDF 재추출과 당시 저장 원문 비교: 1회차 22개, 2회차 39개 모두 일치, 불일치 0건.
검증 대상은 잘린 검색 청크가 아닌 원문 페이지 전체였으므로 청크 경계만으로 거부된 정황 없음.
다만 PDF 자체의 읽기 순서나 표 추출 품질까지 완전하다는 의미는 아님.
과거 거부 인용문은 저장되지 않았으므로 모든 과거 실패를 같은 원인으로 확정하지 않음.

발췌 ID는 source_id와 원문 위치 및 텍스트에 연결된 해시로 생성.
다른 출처의 발췌 ID나 존재하지 않는 ID는 거부, 의미 유사도에 의한 인용 허용 없음.
한 주장에 여러 발췌가 필요하면 근거 카드를 분리하고 evidence_ids로 연결.
원문 텍스트 일치와 주장의 사실성은 별도이며, 후속 Verification 검증 유지.

자동 검증: [인용 회귀 테스트](../테스트/실행결과/2026-09-22-citation-pytest.txt).
실제 재현의 원문 포함 응답은 Git 제외 경로 `outputs/quote-diagnosis/`에 보관.

### 터미널 진행 화면 추가

- 개별 호출 시간 추가: LLM, 검색, 원문 읽기의 `call_start`, `call_end` 이벤트를 고유 call_id로 연결
- 에이전트, 작업 ID, 호출 회차, 작업 종류, 성공/실패, 소요 시간 기록. 프롬프트와 원본 오류 본문 미기록
- 실행 중 호출 경과 시간, 종류별 최근/평균/누적/최대 시간 표시. 실패 호출 포함, 병렬 누적 시간과 실제 경과 시간 구분
- 에이전트 전체 소요 시간은 기존 `agent_return.elapsed_seconds`에 유지
- 검증: [호출 시간 테스트](../테스트/실행결과/2026-09-22-call-timing-pytest.txt)

- 호출 예산 표시 추가: 실제 CallBudget의 사용량, 한도, 남은 호출 및 종류별 사용 횟수
- 작업 시간 한도 및 남은 시간 표시, 호출 80% 이상 노란색, 한도 소진 시 빨간색 안내
- 병렬 실행 중 잠금으로 일관된 집계 조회, 화면 조회에 따른 호출 차감 없음
- 초기 모델 준비 시간은 작업 예산 시작 전이므로 전체 경과 시간과 작업 시간 구분
- 검증: [호출 한도 표시 테스트](../테스트/실행결과/2026-09-22-budget-display-pytest.txt)

- `src/common/terminal.py`: Rich 상태 박스, 4Hz 화면 갱신, 실행 중 스피너, 단계별 상태와 호출 회차 표시
- 병렬 평가와 근거 검증의 실제 처리 건수 기반 진행 막대, 재시도 시작 시 해당 막대 초기화
- `main.py --plain`으로 기존 로그 선택, 비대화형 출력 자동 전환, `--quiet` 유지
- 마스킹된 이벤트만 화면에 전달, JSONL 상세 로그 유지, 중단 시 터미널 복원
- 자동 테스트: [실행 결과](../테스트/실행결과/2026-09-22-terminal-ui-pytest.txt)
- 터미널 데모 실행 확인, 실제 유료 전체 조사 실행 없음
- 적용 범위: `main.py` 실행 화면. pytest, 인덱스 생성, API smoke는 각 명령의 기존 출력 유지

| ID | 상태 | 증상 | 원인 / 판단 | 수정 및 다음 조치 |
|---|---|---|---|---|
| DBG-001 | 해결 | 이전 LLM 요청 HTTP 400, invalid model ID | 표시 이름과 API 모델 ID 불일치 | 현재 설정으로 일반 응답 성공. API 모델 ID는 `gpt-5.6-luna` |
| DBG-002 | 해결 | 일반 응답은 성공하지만 ResearchOutput 함수 호출은 HTTP 400 | 서버가 Luna의 Chat Completions에서 추론과 함수 도구 조합을 거부 | `src/config.py`에서 Luna를 Responses API로 연결. 일반 응답과 실제 ResearchOutput 검증 모두 PASS |
| DBG-003 | 해결 | 자동 테스트 5개 실패, PDF 관련 5개 건너뜀 | macOS 기본 한글 폰트가 자동 탐색 목록에 없었음 | `src/exporters/pdf.py`에 기본 AppleGothic 경로 추가. 별도 폰트 설정 없이 69개 통과, 1개 건너뜀. 렌더링 한글 표시 확인 |
| DBG-004 | 해결 | 지원 범위 밖 Python에서 검증 | 기존 sllm은 Python 3.14, 프로젝트는 >=3.11,<3.14 | 사용자 요청으로 >=3.14,<3.15 및 `.python-version=3.14`로 변경. uv.lock 재생성, Python 3.14.6 `.venv`에 dev/rag 실제 설치, 의존성 검사 통과 |

## DBG-002 증거와 변경

서버 응답: `Function tools with reasoning_effort are not supported for gpt-5.6-luna in /v1/chat/completions.`
서버가 제안한 Responses API를 사용하여 추론을 유지했습니다.
다른 모델의 기존 연결 선택은 유지합니다.

- 실패: [API 이전 결과](../테스트/실행결과/20260921T212931899724Z-api.json)
- 성공: [API 수정 후 결과](../테스트/실행결과/20260921T213016379118Z-api.json)
- 회귀 검사: `tests/test_openai_adapter.py::test_luna_routes_function_tools_through_responses_api`
- [기존 전체 테스트](../테스트/실행결과/2026-09-22-pytest.txt), [수정 후 전체 테스트](../테스트/실행결과/2026-09-22-pytest-after.txt)

실제 호출은 LLM 일반 응답, ResearchOutput 스키마 파싱, Tavily 결과 수신까지 검증했습니다.
논문 RAG, 모든 에이전트 스키마, 실제 전체 파이프라인, 최종 PDF 품질은 검증 범위에 포함하지 않았습니다.

## 후속 기록 양식

Python 3.14 전환 후 결과: [나눔고딕 지정 테스트](../테스트/실행결과/2026-09-22-python314-nanum-pytest.txt).
검증 명령: `PDF_FONT_PATH=/tmp/skala-NanumGothic-Regular.ttf .venv/bin/python -m pytest -q --tb=short`.
`uv lock --check`, `uv pip check --python .venv/bin/python`도 통과했습니다.
임시 폰트 파일은 운영 설정으로 사용하지 말고 원하는 영구 경로에 저장한 뒤 지정해야 합니다.

```text
ID / 발견일:
상태:
재현 명령:
기대 결과 / 실제 결과:
원인과 근거:
수정 파일과 내용:
재검증 결과 파일:
남은 조치:
```

## 기본 폰트 적용 검증

2026-09-22: macOS 기본 AppleGothic을 자동 선택하도록 수정했습니다. `.env` 변경 없이 현재 설정에서도 AppleGothic 선택을 확인했습니다.
[테스트 결과](../테스트/실행결과/2026-09-22-system-font-pytest.txt): 69 passed, 1 skipped. 건너뛴 항목은 DejaVu 폰트가 필요한 비한글 폰트 거부 검사입니다.
한글 제목, 본문, 표가 있는 테스트 PDF를 렌더링하여 깨짐 없이 표시됨을 확인했습니다.

## 인용 수정 후 실제 API 검증

동일 실행의 저장 자료로 총 2회 LLM 호출. 이전 방식 6개 중 원문 일치 0개, 수정 방식 8개 중 8개 수용.
수용된 8개 모두 PDF 페이지를 다시 열어 독립 대조 통과. 자동 테스트 74 passed, 1 skipped.
[비교 결과](../테스트/실행결과/2026-09-22-citation-live-comparison.json).
프롬프트와 페이지 중복 제거를 함께 변경한 제한된 재현이며, 전체 파이프라인 완주나 각 주장의 의미 검증 결과는 아님.
